import React from 'react';
import '../Styles/Home.css'; // Import the CSS for styling the spinner

const Spinner = () => {
  return <div className="buttonload">
    <i class="fa fa-spinner fa-spin"></i>
  </div>;
};

export default Spinner;
